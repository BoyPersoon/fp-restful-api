{
    "title": "Query with invalid where includes tries to operate on Number Key ",
    "query": "In courses dataset courses, find entries whose Average includes \"nsdf\"; show Department and Pass; sort in ascending order by Pass.",
    "response": {
        "code": 400,
        "body": {
            "error": "IS requires a string key"
        }
    }
}
